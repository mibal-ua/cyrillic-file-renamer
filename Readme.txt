author: @mibal_ua
#StandWithUkraine

Important info:

- You can choose between russian `ru` and Ukrainian `ua` languages;
- For use `this` scripts you must move unzipped folder with script (for example `cyrillic-file-renamer-1.0`) to target folder (both with cyrillic files).